# Copy selected settings to ~/.config/neon/local.bash (never tracked here).
# Keep credentials in your normal credential manager, not in this repository.

# Personal style at work, if desired:
# export NEON_ARENA_QUIET=0

# Entirely disable the extra HUD/sounds while retaining other shell tools:
# export NEON_ARENA=0

# Use normal Bash history without Atuin:
# export NEON_ATUIN=0

# Start Neovim with plain Kickstart (no Neon overlay):
# export NEON_NVIM_OVERLAY=0

# Add local-only command paths using an idempotent helper:
# _neon_path_prepend "$HOME/my-machine-tools/bin"

# Set Git identity in ~/.gitconfig, not here. Work identity stays on work machine.
