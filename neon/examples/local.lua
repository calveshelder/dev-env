-- Copy to ~/.config/neon/local.lua. This file belongs to this computer only.
-- These options affect the additive Neon layer, not your Kickstart fork.
return {
  hud = true,     -- false starts with Kickstart's statusline; :NeonHud still works.
  keymaps = true, -- false adds commands only, preserving all existing keymaps.
}

-- Configure your font, relative numbers, formatting and plugins in the
-- Kickstart submodule's init.lua (or its own local customization mechanism).
